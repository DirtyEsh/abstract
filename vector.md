
[[Итератор]]
Ок, я **привёл это в порядок**, убрал кашу, выровнял смысл, ничего не выкинул, добавил недостающие связки. Ниже — **чистый конспект про `std::vector`** в Markdown, готовый к использованию.

---

# `std::vector` — память, размер, вместимость, итераторы

```cpp
#include <vector>
#include <iostream>
```

---

## Что хранит `std::vector` на самом деле

```cpp
std::vector<double> vec;
```

`std::vector` — это **не данные**, а **объект-менеджер**.

Он хранит:

- указатель на данные в куче
    
- `size`
    
- `capacity`
    

```cpp
std::cout << sizeof(vec) << std::endl;               // ~24 байта (объект)
std::cout << vec.size() * sizeof(double) << std::endl; // память под данные
```

📌 **Данные всегда лежат в куче**, сам объект — маленький.

---

## `size()` vs `capacity()`

```cpp
vec.push_back(1.0);
vec.push_back(2.0);
vec.push_back(3.0);

std::cout << vec.size();     // 3
std::cout << vec.capacity(); // >= 3
```

- `size()` — сколько элементов реально есть
    
- `capacity()` — сколько **можно** хранить без реаллокации
    

---

## Реаллокация (важно)

Если `capacity()` заканчивается:

- выделяется новый блок памяти
    
- все элементы копируются / перемещаются
    
- **ВСЕ итераторы инвалидируются**
    

---

## Итераторы `std::vector`

- Итератор — это по сути **указатель**
    
- Любая реаллокация → **все итераторы мертвы**
    
- `resize()` при уменьшении → итераторы после границы мертвы
    

---

## Создание вектора

### Пустой

```cpp
std::vector<int> numbers{};
```

### С размером

```cpp
std::vector<int> numbers(10);        // 10 нулей
std::vector<std::string> s(10, "Тик-так");
```

### Через initializer_list

```cpp
std::vector<int> v = {1,2,3,4,5};
```

### Копирование

```cpp
std::vector another = v;
```

---

## ⚠️ Осторожно с auto-выводом типов

```cpp
std::vector v = {1, 2, 3};  // vector<int> ✅
std::vector s = {"Аб", "Абыр"}; // vector<const char*> ❌
```

Если нужны строки:

```cpp
std::vector<std::string> s = {"Аб", "Абыр"};
```

---

## Основные методы

### Добавление

```cpp
queue.push_back("Первый");
```

### Удаление последнего

```cpp
queue.pop_back();   // ❌ если empty() → UB
```

### Проверка

```cpp
if (!queue.empty()) {
    queue.pop_back();
}
```

### Очистка

```cpp
queue.clear();
```

---

## Доступ к элементам

### `[]` vs `at()`

- `v[i]` — без проверок → **UB**
    
- `v.at(i)` — проверяет → `std::out_of_range`
    

```cpp
v[i];
v.at(i);
```

**Правило:**  
внешний индекс → `at()`  
внутренний → `[]`

---

### Первый и последний элемент

```cpp
v.front(); // первый
v.back();  // последний
```

---

## `resize()` — изменение размера

### Синтаксис

```cpp
vec.resize(n);
vec.resize(n, value);
```

### Примеры

```cpp
std::vector<int> v = {1,2,3};

v.resize(5);      // {1,2,3,0,0}
v.resize(5, 42);  // {1,2,3,42,42}
v.resize(2);      // {1,2}
v.resize(0);      // {}
```

### Важное

- Увеличение → может быть реаллокация
    
- Уменьшение → элементы уничтожаются
    
- Новые элементы → **value-initialization**
    

---

## `reserve()` vs `resize()`

|Метод|Что делает|
|---|---|
|`reserve(n)`|Меняет **capacity**, size не трогает|
|`resize(n)`|Меняет **size**, создаёт/удаляет элементы|

```cpp
vec.reserve(10); // size не меняется
vec.resize(10);  // size = 10
```

❌ `reserve()` **НЕ УМЕНЬШАЕТ** память  
✅ Для уменьшения:

```cpp
vec.shrink_to_fit();
// или
std::vector<int>(vec).swap(vec);
```

---

## Зачем `reserve()`

```cpp
std::vector<int> v;
v.reserve(1000);

for (int i = 0; i < 10; ++i)
    v.push_back(i);
```

- без реаллокаций
    
- быстрее
    
- безопаснее для итераторов
    

---

## Типовой паттерн (правильный)

```cpp
std::vector<std::string>
GetStudentNames(const std::vector<Student>& students) {
    std::vector<std::string> result;
    result.reserve(students.size());

    for (const auto& s : students)
        result.push_back(s.name);

    return result;
}
```

---

## Итоговая выжимка

- `vector` хранит **указатель + size + capacity**
    
- Данные всегда в куче
    
- Реаллокация = смерть итераторов
    
- `reserve()` — для скорости
    
- `resize()` — для логики
    
- `pop_back()` без `empty()` — UB
    
- `[]` быстро, `at()` безопасно
    

Если хочешь, следующий логичный шаг —  
**инвалидация итераторов (таблица: что убивает что)** или **emplace vs push_back**.