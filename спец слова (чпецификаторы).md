[[explicit]]
# Все ходовые спецификаторы C++ с краткими описаниями

## 🏷️ **Спецификаторы доступа (Access Control)**
```cpp
class Example {
public:           // Доступ отовсюду
    int x;        // Все могут видеть/использовать
    
protected:        // Доступ из класса и наследников
    int y;        // Только для семейства класса
    
private:          // Доступ только из этого класса
    int z;        // Секретные данные класса
};
```

## 🔧 **Спецификаторы функций-членов**

### `virtual` - Виртуальная функция
```cpp
virtual void foo();  // Может быть переопределена в наследниках
```

### `override` - Переопределение (C++11)
```cpp
void foo() override; // Явно указывает, что переопределяет virtual-функцию
```

### `final` - Запрет переопределения (C++11)
```cpp
void foo() final;    // Никто не может дальше переопределить эту функцию
```

### `static` - Статический член
```cpp
static int count;    // Общая для всех объектов, существует без объекта
```

### `const` - Константный метод
```cpp
int get() const;     // Не изменяет объект, можно вызывать у const объектов
```

### `volatile` - Volatile метод
```cpp
void read() volatile; // Для работы с volatile объектами (аппаратура, многопоточность)
```

### `mutable` - Изменяемый в const
```cpp
mutable int cache;   // Можно менять даже в const-методах
```

## ⚡ **Спецификаторы времени выполнения**

### [[explicit]] - Запрет неявных преобразований
```cpp
explicit MyClass(int x); // Только явное создание: MyClass obj(5);
```

### `constexpr` - Вычисления при компиляции (C++11)
```cpp
constexpr int square(int x) { return x*x; } // Можно вычислить при компиляции
```

### `consteval` - Только compile-time (C++20)
```cpp
consteval int always_compile(int x) { return x*x; } // Только при компиляции
```

### `constinit` - Статическая инициализация (C++20)
```cpp
constinit static int x = 42; // Гарантированная статическая инициализация
```

## 🎯 **Спецификаторы управления памятью**

### `inline` - Подстановка функции
```cpp
inline void foo() { /* код */ } // Компилятор пытается встроить код
```

### `extern` - Внешнее связывание
```cpp
extern int global_var; // Переменная определена в другом файле
```

### `static` (для глобальных переменных)
```cpp
static int local_to_file; // Видна только в этом файле
```

## 🔄 **Спецификаторы классов и структур**

### `final` для класса (C++11)
```cpp
class Base final { }; // Запрещает наследование от этого класса
```

## 🧬 **Спецификаторы исключений (устаревают в C++17+)**

### `noexcept` - Гарантия без исключений (C++11)
```cpp
void foo() noexcept; // Обещаю не кидать исключения
```

### `throw()` - Устаревший аналог noexcept
```cpp
void bar() throw();  // Старый стиль (deprecated)
```

## 🏗️ **Спецификаторы конструкторов**

### `default` - Сгенерировать по умолчанию (C++11)
```cpp
MyClass() = default; // Компилятор, сгенерируй конструктор сам
```

### `delete` - Удалить функцию (C++11)
```cpp
MyClass(const MyClass&) = delete; // Запретить копирование
```

## 📦 **Спецификаторы шаблонов (C++11+)**

### `typename` / `class` в шаблонах
```cpp
template<typename T>  // или template<class T>
```

### `requires` - Концепты (C++20)
```cpp
template<typename T>
requires std::integral<T> // T должен быть целочисленным
```

## 🔍 **Ходовые комбинации**

### Самые популярные:
```cpp
class Widget {
public:
    // 1. Виртуальный интерфейс
    virtual void draw() const = 0;
    
    // 2. Константный геттер
    int value() const noexcept { return val; }
    
    // 3. Запрет копирования
    Widget(const Widget&) = delete;
    Widget& operator=(const Widget&) = delete;
    
    // 4. Конструктор только явный
    explicit Widget(int v) : val(v) {}
    
private:
    int val;
    mutable int cache; // Можно менять даже в const-методах
};
```

### Для функций:
```cpp
// 1. Чистая виртуальная (абстрактная)
virtual void foo() = 0;

// 2. Константная с гарантией без исключений
int get() const noexcept;

// 3. Статическая утилита
static void helper();

// 4. Вычисляемая при компиляции
constexpr int compute(int x);
```

## 📊 Таблица: Когда что использовать

| Спецификатор | Когда использовать | Пример |
|-------------|-------------------|--------|
| `public` | Интерфейс класса | `void print() public;` |
| `private` | Внутренняя реализация | `int data private;` |
| `const` | Геттеры, не меняющие состояние | `int get() const;` |
| `virtual` | Полиморфное поведение | `virtual void draw();` |
| `override` | Переопределение базового метода | `void draw() override;` |
| `final` | Запрет дальнейшего переопределения | `void draw() final;` |
| `explicit` | Конструкторы с 1 параметром | `explicit Vector(int size);` |
| `noexcept` | Функции без исключений | `void swap() noexcept;` |
| `constexpr` | Вычисляемое при компиляции | `constexpr int pow2(int n);` |
| `mutable` | Кеши, счетчики в const-методах | `mutable int call_count;` |
| `= default` | Нужно поведение по умолчанию | `~MyClass() = default;` |
| `= delete` | Запретить автоматическую генерацию | `MyClass(const MyClass&) = delete;` |

## 🎯 Правило большого пальца:
1. **Конструкторы с одним параметром** → `explicit`
2. **Геттеры** → `const`
3. **Виртуальные функции** → либо `virtual`, либо `override`
4. **Утилиты без состояния** → `static`
5. **Вычисления известные при компиляции** → `constexpr`
6. **Функции без исключений** → `noexcept`

```cpp
// Идеальный класс по современным стандартам
class ModernClass {
public:
    explicit ModernClass(int value) noexcept : data(value) {}
    
    // Геттер
    int get() const noexcept { return data; }
    
    // Чисто виртуальная функция
    virtual void process() const = 0;
    
    // Запрет копирования
    ModernClass(const ModernClass&) = delete;
    ModernClass& operator=(const ModernClass&) = delete;
    
    // Разрешение перемещения
    ModernClass(ModernClass&&) = default;
    ModernClass& operator=(ModernClass&&) = default;
    
    ~ModernClass() = default;
    
private:
    int data;
    mutable int access_count = 0; // Считаем обращения даже из const-методов
};
```


```
int main(){
	std::cout
}
```

---
---
`unsigned` — это ключевое слово в языках C, C++, C# и других, которое указывает, что целочисленная переменная может хранить **только неотрицательные числа** (ноль и положительные).

Давайте разберем подробно.

### **Простыми словами:**

- **`int` (по умолчанию `signed int`)** — это знаковое целое. Может быть отрицательным, нулем или положительным. Диапазон симметричен относительно нуля.
    
- **`unsigned int`** — это **беззнаковое** целое. Может быть только нулем или положительным. За счет отказа от отрицательных чисел оно может хранить **вдвое большие положительные значения**.
  
  #include <iostream>
#include <vector>

int main() {
    std::vector<int> vec = {1, 2, 3, 4, 5};
    
    int signed_index = -1;           // Обычный int, может быть отрицательным
    size_t unsigned_index = 0;       // size_t — это и есть unsigned (для размеров)
    
    // 🔥 ОПАСНОСТЬ №1: сравнение signed с unsigned
    if (signed_index < vec.size()) { // vec.size() возвращает size_t (unsigned!)
        std::cout << "Это НЕ напечатается! Потому что -1 конвертится в огромное unsigned число\n";
    } else {
        std::cout << "А это напечатается! Вот тебе и баг\n";
    }
    
    // 🔥 ОПАСНОСТЬ №2: цикл с signed
    for (int i = 0; i < vec.size() - 10; i++) { // vec.size() - 10 = огромное число!
        // Этот цикл может запуститься, хотя не должен!
    }
    
    return 0;
}