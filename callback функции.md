**Callback** — функция, переданная в другую функцию/объект и вызываемая позже.
Ключевая идея: _код вызывает не ты, а тебя_.

#include <functional>

std::function<void(int)> cb; // функциональный объект
[[callable (функциональный объект)]]

cb = [](int x) { std::cout << x; };

cb(10);