Ок, **только эта часть**, приведённая в порядок, без лишнего, Markdown чистый.

---

# `std::reverse`, `unique/remove`, случайные числа и `shuffle`

## `std::reverse`

Разворачивает диапазон **на месте**.

```cpp
#include <algorithm>

std::reverse(teams.begin(), teams.end());
```

- Работает за `O(n)`
    
- Не меняет размер контейнера
    
- Итераторы **остаются валидными** (для `vector`)
    

---

## `std::unique` и erase–remove idiom

### `std::unique`

- **НЕ удаляет элементы**
    
- Сдвигает дубликаты в конец
    
- Возвращает итератор на новый логический конец
    

```cpp
#include <algorithm>

auto to_del = std::unique(some_vec.begin(), some_vec.end());
some_vec.erase(to_del, some_vec.end());
```

⚠️ Важно: `unique` убирает **только соседние** дубликаты  
→ обычно перед ним делают `sort`.

---

## `std::remove` и `std::remove_if`

Работают **аналогично `unique`**:

- ничего не удаляют
    
- переставляют “лишние” элементы в конец
    
- возвращают итератор, с которого нужно делать `erase`
    

### Пример

```cpp
std::vector<int> vec = {4, 2, 6, 3, 1, 5, 3, 2, 15, 0, 3, 1};

// Удаляем все двойки
auto to_del = std::remove(vec.begin(), vec.end(), 2);
vec.erase(to_del, vec.end());
// {4, 6, 3, 1, 5, 3, 15, 0, 3, 1}

// Удаляем нечётные
auto to_del2 = std::remove_if(vec.begin(), vec.end(),
    [](int i){ return i % 2 != 0; });
vec.erase(to_del2, vec.end());
// {4, 6, 0}
```

📌 **Запомнить**:  
`remove / unique` → всегда **+ erase**

---

# Псевдослучайные числа (`<random>`)

## Генератор `std::mt19937` (вихрь Мерсенна)

```cpp
#include <random>

std::mt19937 gen;
```

- Псевдослучайный
    
- Без seed → **одна и та же последовательность**
    

---

## Распределение `std::uniform_int_distribution`

Преобразует “сырые” числа генератора в **равномерные** в заданном диапазоне.

```cpp
std::uniform_int_distribution<> dist(1, 1'000'000);
```

### Пример заполнения вектора

```cpp
std::vector<int> random_numbers(5);

for (auto& num : random_numbers) {
    num = dist(gen);
}
```

---

## Seed (сид)

### Фиксированный сид — воспроизводимость

```cpp
std::mt19937 gen(1);
```

Последовательность всегда одинаковая.

---

### Случайный сид через `std::random_device`

```cpp
std::random_device rd;
std::mt19937 gen(rd());
```

- Медленно
    
- Не для логики
    
- **Подходит для seed**
    

---

## Перемешивание: `std::shuffle`

```cpp
#include <algorithm>
#include <random>

std::vector<int> numbers{1,2,3,4,5,6};

std::random_device rd;
std::mt19937 gen_shuffle(rd());

std::shuffle(numbers.begin(), numbers.end(), gen_shuffle);
```

---

## Выжимка (для конспекта)

- `reverse` — разворачивает диапазон
    
- `unique/remove` **не удаляют**, только переставляют
    
- Всегда: `erase(new_end, end)`
    
- `mt19937` — быстрый PRNG
    
- `uniform_int_distribution` задаёт диапазон
    
- `random_device` — только для seed
    
- `shuffle` требует генератор
    

Если нужно — следующим шагом можно сделать **таблицу инвалидации итераторов** для всех этих алгоритмов.




|                | `random_device` | `mt19937`   |
| -------------- | --------------- | ----------- |
| Возвращает     |                 |             |
| Возвращает     | одно число      | поток чисел |
| Состояние      | нет             | есть        |
| Детерминирован | нет*            | да          |
| Назначение     | seed            | gen         |
| Повторяемость  | неи             | да          |
