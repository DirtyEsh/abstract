**`setEnabled()`:**  - Активирование кнопок

```
cpp

// Делает активной (кликабельной)
ui->button->setEnabled(true);  

// Делает неактивной (серой, не кликается)
ui->button->setEnabled(false);

```
---
